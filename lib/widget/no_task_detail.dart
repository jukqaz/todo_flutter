import 'package:flutter/material.dart';

class NoTaskDetail extends StatelessWidget {
  const NoTaskDetail({super.key});

  @override
  Widget build(BuildContext context) => const Center(child: Text('No task detail'));
}
